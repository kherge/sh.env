            nvm  Adds support for NVM by downloading and installing it if it is
                 not available. Once installed, the NVM shell script is loaded
                 each time the shell is loaded.
