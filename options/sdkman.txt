         sdkman  Adds support for SDKMAN! by downloading and installing it if
                 it is not available. Once installed, the SDKMAN! shell script
                 is loaded each time the shell is loaded.
